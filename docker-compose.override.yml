version: "2.3"

services:
  cvat:
    environment:
      ALLOWED_HOSTS: "*"
      CVAT_SHARE_URL: "Mounted from /s3"
    ports:
      - "8080:80"
    volumes:
      - cvat_share:/home/django/share:ro

volumes:
  cvat_share:
    driver_opts:
      type: none
      device: /home/ubuntu/mys3bucket
      o: bind